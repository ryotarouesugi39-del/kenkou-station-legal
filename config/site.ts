/**
 * ============================================================
 *  サイト共通設定（全アプリ共通・唯一の真実の情報源）
 * ============================================================
 *
 * 株式会社トイマンが運営する全アプリに共通する情報（運営者名義・
 * 連絡先・ドメイン）を管理します。このファイルを編集すれば、
 * 全アプリの全ページに反映されます。
 *
 * アプリごとの情報（アプリ名・ポリシー日付など）は config/apps.ts
 * を参照してください。
 *
 * 事業譲渡などで運営者が変わった場合は、README.md の
 * 「運営者変更時の手順」を参照してください。
 * ============================================================
 */

export const siteConfig = {
  /** サイト全体のタイトル（複数アプリのポータルとしての名称） */
  siteName: "株式会社トイマン",

  /** 運営会社・運営者名（個人の場合は「◯◯（個人事業主）」などでも可） */
  operatorName: "株式会社トイマン",

  /** 問い合わせ先メールアドレス（全アプリ共通） */
  contactEmail: "info@toiman.co.jp",

  /** サイトの本番ドメイン（https を含めない） */
  domain: "toiman.co.jp",

  /** サイトのベースURL（https を含む） */
  get siteUrl() {
    return `https://${this.domain}`;
  },
} as const;

export type SiteConfig = typeof siteConfig;
